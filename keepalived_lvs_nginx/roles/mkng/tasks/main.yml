- name: 安装nginx
  yum:
    name: "{{ item }}"
  with_items:
    - "nginx"

- name: 拷贝配置文件
  template:
    src: "nginx.conf.j2"
    dest: /etc/nginx/nginx.conf

- name: 把nginx拉起和加到开机启动
  service:
    name: nginx
    state: started
    enabled: yes
